name: Build and Cache Docker image

on:
  push:
    branches: [ master, SOFTWARE-4730 ]
  repository_dispatch:
    types:
      - dispatch-build
  workflow_dispatch:

jobs:
  build-cache-image:
    runs-on: ubuntu-latest
    if: startsWith(github.repository, 'yongleyuan/')
    strategy:
      fail-fast: False
      matrix:
        repo: ['development', 'testing', 'release']
    steps:
    - uses: actions/checkout@v2

    - uses: yongleyuan/build-container-action@v1
      with:
        repo: ${{ matrix.repo }}
